k = '5 4 3 2 1'
k = k.split()
print k

for i in range( len(k)-1 ):
          for j in range( len(k)-i-1 ):
                    if k[j] > k[j+1]:
                              temp = k[j]
                              k[j] = k[j+1]
                              k[j+1] = temp
print k
